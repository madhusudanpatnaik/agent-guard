# Runtime dependencies for AgentOps.
# Pinned to floors known to work on Python 3.11–3.14.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
cryptography>=42  # credential vault (encrypts connector secrets at rest)
alembic>=1.13     # database schema migrations
sqlglot>=25.0     # AST-based SQL governance (replaces text classification)

# Optional: PostgreSQL driver (used by docker-compose; SQLite needs nothing extra)
psycopg[binary]>=3.1

# Dev / test
pytest>=8.0
pytest-cov>=5.0
